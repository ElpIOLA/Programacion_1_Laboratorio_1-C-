#include <stdio.h>
#include <stdlib.h>

int main()
{
    int *vector, *vector_convertido;
    int i;

    vector = (int *)malloc(3*sizeof(int)); //Reserrvando memoria para 3 elementos
    return 0;
}
